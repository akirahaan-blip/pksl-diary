# 公開のようす・更新のしかた（GitHub Pages）

## いまの状態

**すでに公開ずみです。** 2026-09-11 に Claude が GitHub へアップロードして、公開のスイッチも入れました。

- 公開URL： **https://akirahaan-blip.github.io/pksl-diary/**
- ファイルの置き場所（リポジトリ）： https://github.com/akirahaan-blip/pksl-diary
- 検索避け（noindex）の設定を入れてあるので、Google などの検索には出ません。
  URL を知っている人だけが開けます。

### 公開されているもの／されていないもの

- 公開されている： `index.html`（アプリ本体）、アイコン、`README.md`、起動用の bat ファイル
- 公開されていない： テスト用のスクショ（`test1〜3.png`、`calendsa.png`）。
  `.gitignore` というファイルで「上げない」設定にしてあります。
- **日記の中身（スクショや文章）は、GitHub には一切送られません。**
  あなたのスマホ（ブラウザ）の中だけに保存されます。

---

## スマホで使い始める手順

1. スマホのブラウザ（iPhone なら Safari、Android なら Chrome）で
   `https://akirahaan-blip.github.io/pksl-diary/` を開く
   → ピンクの画面に「9月○日（○）今日」と出ればOK
2. **ホーム画面に追加**すると、アプリみたいに1タップで開けます。
   - iPhone（Safari）：画面下の「共有」ボタン（□に↑）→ 下にスクロール →
     「ホーム画面に追加」→ 右上「追加」
   - Android（Chrome）：右上の「⋮」→「ホーム画面に追加」（または「アプリをインストール」）
   → ホーム画面に「ポケスリダイアリー」のピンクのアイコンが出ればOK
3. 以後はそのアイコンから開く。

> **大事：** 「Safari で開いたとき」と「ホーム画面のアイコンから開いたとき」は、
> iPhone では**別の保存場所**になることがあります。
> **どちらか一方に決めて、ずっとそっちで使ってください。**（おすすめはホーム画面のアイコン）

---

## バックアップのすすめ

日記はブラウザの中に保存されるので、次のようなときに消えます。

- ブラウザの「履歴とサイトデータを消去」をしたとき
- スマホを買い替えたとき
- ホーム画面のアイコンを削除したとき（iPhone）

なので、**月に1回くらい、設定 →「📤 書き出す」**でバックアップファイルを作って、
iCloud / Google ドライブ / 写真フォルダなどに置いておいてください。
戻すときは 設定 →「📥 読み込む」でそのファイルを選ぶだけです。

---

## アプリを直したとき（更新のしかた）

パソコンの `index.html` を直しただけでは、公開中のサイトは変わりません。
GitHub に「アップロードし直す」必要があります。

**Claude に「公開に反映して」と言えば、Claude がやります。** 自分でやる場合は：

1. PowerShell を開く（スタートボタンを右クリック → 「ターミナル」）
2. 次の3行を1行ずつ貼り付けて、それぞれ Enter

   ```
   cd "C:\Users\oauao\Claude\ポケスリ\pksl-diary"
   git add -A; git commit -m "直した内容をここに書く"
   git push
   ```

3. 1〜2分待って、スマホでページを開き直す（下に引っぱって更新）
   → 直した内容が反映されていればOK

> スマホで古いままに見えるときは、いったんブラウザを完全に閉じてから開き直してください。

---

## 困ったときのチェックリスト

| 症状 | 確認すること |
|---|---|
| URL を開くと「404」 | 公開直後は数分かかります。少し待って開き直す。それでもダメなら Claude に伝える |
| 画面が真っ白 | 通信が切れていないか。iPhone のプライベートブラウズになっていないか |
| 保存できない／消えた | プライベートブラウズ（シークレット）だと保存できません。通常のウィンドウで開く |
| 「共有する」ボタンが出ない | パソコンのブラウザには出ません（スマホ専用）。「画像を保存」を使う |
| 絵日記の文字がゴシック体っぽい | フォント（M PLUS 1p）の読み込み前に作られた可能性。もう一度「絵日記にする」を押す |
